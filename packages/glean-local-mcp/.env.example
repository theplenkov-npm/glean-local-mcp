# Glean OAuth Configuration
# Set these as environment variables (via nx, node --env-file, or your IDE)
GLEAN_CLIENT_ID=your_client_id_here
GLEAN_CLIENT_SECRET=your_client_secret_here

# OAuth Issuer URL (supports OIDC discovery)
# Examples:
#   Okta: https://your-company.okta.com
#   Auth0: https://your-tenant.auth0.com
#   Keycloak: https://auth.your-company.com/realms/your-realm
OAUTH_ISSUER_URL=https://your-company.okta.com

# Glean API Configuration
GLEAN_API_BASE_URL=https://your-company.glean.com

# Optional: OAuth callback configuration
# REDIRECT_URI=http://localhost:8080/authorization-code/callback
# OAUTH_PORT=8080

# Optional: OAuth scopes (comma-separated, defaults to: openid,email,profile,offline_access)
# OAUTH_SCOPES=openid,email,profile,offline_access

# Optional: Token storage path (defaults to ~/.glean/tokens.json)
# TOKEN_STORAGE_PATH=~/.glean/tokens.json
